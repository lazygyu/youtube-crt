{
  "name": "youtube-tube",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build ./src/index.ts --out-dir dist && npm run copy-resources",
    "copy-resources": "copyfiles --verbose \"./manifest.json\" \"./assets/**/*\" dist",
    "dev": "parcel watch ./src/index.ts --out-dir dist"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "copyfiles": "^2.4.1",
    "parcel-bundler": "^1.12.5",
    "typescript": "^5.3.3"
  }
}
